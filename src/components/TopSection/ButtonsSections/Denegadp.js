import Button from "@/components/Button/Button";
import React from "react";

const Denegado = () => {
  return (
    <Button
      propsStyles={{
        color: "white",
        backgroundColor: "#D84599",
        border: "none",
        fontSize: "small",
      }}
    >
      Denegado
    </Button>
  );
};

export default Denegado;